# Learning Journal

## Session 001 - 项目立项

日期：2026-03-12

### 我们这一步在做什么

我们没有急着做一个“很强的 Agent”，而是先把目标收敛到一个最小可用版本：

- 做一个带桌面宠物外壳的个人助理
- `v1` 先专注于本地文件检索

### 这一步学到的东西

- `Agent` 不等于“一个会聊天的模型”
- 一个可用的 Agent 至少要有：
  - 明确目标
  - 可调用的工具
  - 可追溯的数据来源
  - 清楚的边界
- 桌面宠物只是表现层，不是核心能力本身
- 真正有价值的能力是：理解意图、找到信息、把结果可靠地交给用户

### 我们做出的关键决定

- `v1` 先不碰密码保存
- `v1` 不扫描整个电脑
- `v1` 只处理用户指定目录
- 技术路线先定为：
  - Electron 做桌面端
  - Python 做 Agent 服务
  - SQLite 做本地存储和检索基础

### 为什么这些决定重要

- 先收窄范围，项目更容易真正做完
- 先解决“找得到文件”这个刚需，比先做花哨交互更有价值
- 安全边界一开始就立住，后面不容易走偏

### 当前项目状态

- 已创建仓库：`C:\\Users\\Administrator\\Desktop\\agent_study`
- 已初始化 Git 仓库
- 已建立共同笔记文件

## Session 002 - 固定工作目录与远程仓库

日期：2026-03-12

### 我们这一步在做什么

把学习项目的本地目录和远程 Git 仓库固定下来，避免后面代码、笔记、版本历史分散在不同地方。

### 这一步学到的东西

- 做长期项目时，尽早固定工作目录很重要
- “共同笔记”和“代码仓库”最好在同一个项目目录里
- 本地 Git 仓库和远程 GitHub 仓库是两件事：
  - 本地仓库负责当前改动管理
  - 远程仓库负责同步、备份和协作

### 我们做出的关键决定

- 后续默认工作目录切换为：`C:\\Users\\Administrator\\Desktop\\agent_study`
- 当前远程仓库设为：`https://github.com/Tiyou-zm/myagent_elf.git`

### 备注

- 远程仓库探测时出现了一次网络重置，所以目前只完成了 `origin` 绑定
- 如果下一步需要拉取或推送，再重新检查远程分支状态

## Session 003 - Git 推送排障与历史整理

日期：2026-03-12

### 我们这一步在做什么

把今天的学习记录真正同步到 GitHub，同时把本地仓库的分支状态整理成后面可继续开发的样子。

### 这一步学到的东西

- “能和大模型对话”不等于“本机 Git 到 GitHub 的传输一定正常”
- 这台机器上：
  - 普通网页访问 GitHub 正常
  - GitHub REST API 正常
  - 但 `git remote-https` 的 smart-HTTP 传输会在认证后被重置
- 所以问题不在仓库权限，而在 Git 这条传输链路本身

### 我们做过的验证

- 检查了 `github.com:443`，网络端口是通的
- 检查了 Git 配置、凭据和代理线索
- 打开了 Git 的详细网络日志
- 确认了：
  - `origin` 配置正确
  - GitHub 凭据可用
  - REST API 可以正常访问仓库
  - 失败点主要发生在 Git 的 smart-HTTP 请求阶段

### 最终处理方式

- 本地先正常提交了今天的笔记
- 当标准 `git push` 失败后，改用 GitHub API 把文件内容同步到了远程仓库
- 为了减少后续混乱，把本地工作分支统一整理为 `main`
- 同时保留一个本地历史备份分支：`codex/master-pre-main-sync`

### 当前结论

- 远程仓库已经有今天的笔记文件
- 本地仓库也保留了完整内容
- 但由于远程首次写入是通过 API 完成的，本地和远程 Git 历史目前不是完全同构
- 这不影响我们继续学习和开发，但要记住这是一笔“已知技术债”

### 为什么要把这件事记下来

- 以后重新开线程时，不会误以为“push 失败是权限问题”
- 我们能快速回忆起这台机器的 Git 特性和已经验证过的路径
- 后续如果网络环境改善，可以再把 Git 历史进一步收敛

## Session 004 - 网络恢复后完成标准 Git 对齐

日期：2026-03-12

### 我们这一步在做什么

在切换网络环境后，重新验证 GitHub 链路，并把本地与远程仓库重新拉回标准 Git 工作流。

### 这一步学到的东西

- 是否“能上网”并不重要，关键是 `git fetch / git push` 这条链路是否真的恢复
- 一旦 `ls-remote` 和 `fetch` 能稳定成功，就可以开始做真正的历史对齐
- 对齐历史前先看文件内容差异，避免误把“只有换行不同”当成真实内容冲突

### 这一步的验证结果

- `git ls-remote` 成功
- `git fetch origin main` 成功
- 本地和远程文件内容差异确认只有换行层面的区别
- 这说明现在可以安全地把本地工作分支重新对齐到远程 `main`

### 最终处理方式

- 保留已有备份分支：`codex/master-pre-main-sync`
- 继续以 `main` 作为本地工作分支
- 用标准 Git 把本地 `main` 对齐到远程 `origin/main`
- 再用一次标准 `git push` 验证链路恢复

### 当前结论

- 这次 Git 问题已经从“只能 API 兜底”推进到了“标准 Git 工作流恢复”
- 后续正常开发、提交、推送都应优先走标准 Git
- 之前那段 API 兜底历史仍然被记录在日记里，作为一次真实的排障案例保留

### 下一步

开始搭 `Python` 索引服务骨架，并把第一版目录结构和 API 设计也同步记进这份日记。

## Session 005 - Python 索引服务骨架落地

日期：2026-03-12

### 我们这一步在做什么

把仓库从“只有项目笔记”推进到“有第一版可扩展后端骨架”，目标不是一次做全，而是先把最小链路真正立起来：

- 扫描用户指定目录
- 抽取文本内容
- 写入 SQLite FTS5 索引
- 提供最小查询 API

### 这一步学到的东西

- “骨架”不是只画目录树，而是要让关键职责先分层站住
- 对这个项目来说，最先稳定的不是 Agent 编排，而是检索底座
- 如果现在就把扫描、存储、搜索混写在一起，后面加摘要、重排、语义检索会很难拆

### 这一步做出的结构决定

- 包目录定为：`src/index_service`
- 当前按 4 层拆开：
  - `api.py`：HTTP 接口
  - `indexing.py`：扫描、读取、切块、建索引
  - `storage.py`：SQLite/FTS5 持久化
  - `search.py`：查询服务
- 配置集中在 `config.py`
- 先只支持常见文本文件，避免一开始就陷入复杂解析兼容性

### 当前最小接口

- `GET /healthz`
- `POST /api/v1/index`
- `POST /api/v1/search`

### 当前仍然刻意不做的事

- 不做整机扫描
- 不做 Office/PDF 等复杂文档解析
- 不做 embedding 和语义召回
- 不做桌面端联调

### 为什么这样推进

- 这样能最快验证“本地文件检索助理”最核心的价值链路
- SQLite FTS5 足够支撑第一版可演示能力
- 后面无论接 Electron，还是增加摘要/语义检索，都有稳定底座可挂接

### 当前结果

- 已补第一版 Python 服务目录
- 已补依赖清单和本地运行说明
- 已补一个覆盖“建索引 -> 搜索”的最小测试

### 下一步

- 先验证 FastAPI 依赖安装和接口可启动
- 再补增量更新、忽略规则、结果排序
- 然后再考虑 Electron 侧如何调用这些接口

## Session 006 - 为代码补中文注释并规划教学流程

日期：2026-03-12

### 我们这一步在做什么

先不继续扩功能，而是把刚落下来的 Python 骨架补成更适合一起学习的版本：

- 给关键代码加中文注释
- 单独写一份后续教学流程
- 保证后面讲代码时有稳定起点

### 这一步做出的调整

- 给配置、索引、存储、搜索、API、入口和测试补了中文注释
- 注释重点放在“为什么这样设计”，而不是逐行翻译代码
- 新增 `TEACHING_FLOW.md`，明确后续准备怎么带着理解这套骨架

### 当前结论

- 代码现在更适合边看边学
- 后面讲解会先从 `storage.py` 和 FTS5 开始
- 这样能先把最关键的底层认知站稳，再往上看索引流程和 API

## Session 007 - 补增量索引、文件检索和根目录查询

日期：2026-03-12

### 我们这一步在做什么

在第一版骨架之上，继续把它往“更像能用的 v1”推进，而不是只停在演示级骨架：

- 给索引流程补最小增量能力
- 给服务补文件名检索
- 给服务补已登记根目录查询

### 这一步学到的东西

- “能建索引”不等于“每次都该从头重建索引”
- 对本地文件检索助理来说，找“文件名”本身就是一条独立需求，不只是找内容
- 目录授权边界除了输入时要控制，服务里也应该能回看当前登记状态

### 这一步做出的实现调整

- 在索引时先读取已有文件元数据
- 如果文件大小和修改时间都没变，就先跳过重建文本块
- 新增基于路径模糊匹配的文件检索
- 新增根目录列表查询接口

### 当前接口

- `GET /healthz`
- `GET /api/v1/roots`
- `POST /api/v1/index`
- `POST /api/v1/search`
- `POST /api/v1/search/files`

### 当前结果

- 第二次索引同一批未变化文件时，会走跳过逻辑
- 服务现在既能按内容找，也能按文件名找
- 测试已覆盖：
  - 内容建索引与搜索
  - 增量索引跳过
  - 文件名检索
  - 根目录登记查询

### 下一步

- 继续优化检索排序
- 补更多文件编码和文档格式支持
- 再决定 Electron 侧第一轮怎么调用这些接口

## Session 008 - 代码讲解与协作节奏校正

日期：2026-03-12

### 我们这一步在做什么

这一步没有继续盲目扩功能，而是把已经写出来的索引服务拿回来讲明白，同时把后续协作节奏重新对齐：

- 先用人话解释当前 `v1` 已经做成了什么
- 再逐个讲清楚增量索引、文件名检索、内容检索、FTS5 和存储层职责
- 最后把“边做边学”和“提交信息格式”固化成项目约定

### 这一步学到的东西

- 增量索引的当前粒度是文件级：文件没变就整篇跳过，文件变了就整篇重切重写
- 文件名检索搜的是路径字符串，内容检索搜的是文本块正文，所以必须分两条路
- 文本切块不是为了形式，而是为了让检索结果更容易定位、展示和解释
- FTS5 不是普通表的替代品，而是给正文搜索提供全文检索能力
- `storage.py` 不是业务主角，它的职责是把所有直接碰 SQLite 的细节收口

### 这一步对协作方式的校正

- 以后默认按“先做一小步，再讲清楚这一小步”的节奏推进
- 讲解时优先用精确可点的文件链接，避免模糊行号增加阅读负担
- 每次推进后都把结果同步回三份核心文档
- 以后所有 Git 提交信息默认写成十几到二十字左右的浓缩简介

### 今天实际完成的过程回顾

- 从 Python 索引服务骨架继续推进到更像可用 `v1` 的状态
- 补了文件级增量索引
- 补了文件名检索和根目录查询
- 通过对话把这些改动背后的原因和分层思路讲清楚
- 现在用户已经能用自己的话描述这些模块在做什么

### 下一步

- 继续保持小步推进
- 尽量一边做一边校验理解，不再只追求代码往前堆
- 后续如需继续扩功能，优先挑一块小能力配套讲清楚

## Session 009 - Git 调试与 SSH over 443 固化

日期：2026-03-12

### 我们这一步在做什么

把今天反复出现的 GitHub 推送不稳定问题彻底查清楚，并把更稳的连接方案落到本机：

- 检查 Git 代理、环境变量代理、WinHTTP 代理
- 检查 DNS 和 `github.com:443` 连通性
- 分别验证普通 HTTPS、`git ls-remote`、`git push`
- 最后切换到 `SSH over 443`

### 这一步学到的东西

- 问题并不像“代理配错”那么简单
- 普通 HTTPS 访问 GitHub 和 `git ls-remote` 可以正常，不代表 `git push` 一定稳定
- 本机更像是对 GitHub HTTPS push 这条链路存在偶发连接重置
- 对这台机器来说，`SSH over 443` 比 HTTPS push 更适合作为默认方案

### 我们做过的排查

- 确认 Git 没有额外的 `http.proxy` / `https.proxy`
- 确认环境变量里没有代理残留
- 确认 WinHTTP 是 direct access
- 确认 `github.com:443`、`github.com:22`、`ssh.github.com:443` 都可以探测
- 抓了 `git push` 的详细日志，确认认证和收发包流程本身能走通，但 HTTPS push 仍有偶发 reset

### 最终处理方式

- 在 `C:\\Users\\Administrator\\.ssh` 下生成专用 GitHub SSH key
- 配置 `C:\\Users\\Administrator\\.ssh\\config`
- 让 `github.com` 通过 `ssh.github.com:443` 建立 SSH 连接
- 把仓库 `origin` 从 HTTPS 改成 `git@github.com:Tiyou-zm/myagent_elf.git`
- 验证 `ssh -T git@github.com` 成功
- 验证 `git push origin main` 成功

### 当前结论

- 这台机器后续应优先使用 SSH 推送 GitHub
- 如果再次遇到推送问题，优先检查 SSH 连通性，而不是先怀疑业务代码
- 今天这次排障结果已经被固化到项目文档，后续不需要从头再查一遍

## Session 010 - 索引主流程与切块机制学习

日期：2026-03-13

### 我们这一步在做什么

今天没有继续加新功能，而是沿着现有代码把“索引到底怎么跑起来”这条链路拆开学清楚：

- 先看 `build_index()` 是怎么调度整条索引流程的
- 再看文件没变为什么可以跳过
- 然后看文件一旦变化，为什么是整文件重切重写
- 最后把 `build_chunks()` 的切块规则讲明白

### 这一步学到的东西

- `build_index()` 的主骨架是：
  - 遍历候选文件
  - 读取文件状态
  - 查旧记录
  - 没变就跳过
  - 变了就读正文、切块、入库
- 当前增量索引判断依赖 `size_bytes + mtime_ns`
- 这里不直接用 `content_hash` 做第一层判断，是因为算 hash 之前就得先把文件读出来，成本更高
- 当前策略不是块级增量更新，而是文件级判断后整文件替换文本块

### 关于切块的理解

- 切块不是按行号切，也不是按句子切
- 主规则是按字符数控制块大小
- 但实现时是一行一行往当前块里加，避免把单行内容硬切断
- 所以块大小是近似控制，可以略微超过阈值
- 行号的作用不是决定怎么切，而是给切好的块做定位标签

### 关于重叠内容的理解

- 一块切完后，会保留块尾的一小段内容给下一块继续用
- 当前重叠大小按字符数配置，而实际保留时按行回收
- 这样做是为了让前后块之间的上下文更连贯，避免命中结果断得太厉害
- 也正因为下一块不是从空白开始，所以要重新计算：
  - 当前块字符数
  - 当前块起始行号

### 这一步形成的关键结论

- 当前索引策略可以概括成一句话：
  - 文件没变就跳过，文件变了就整篇重切重写
- 当前切块策略可以概括成一句话：
  - 按字符数控大小，按行保完整，按重叠保上下文

### 下一步

- 后续可以继续往搜索阶段推进
- 重点看切好的块在 FTS5 搜索里是怎么被命中、打分和返回片段的

## Session 011 - 补新电脑续学说明

日期：2026-03-14

### 我们这一步在做什么

把“以后换一台电脑，怎么无缝接着学”这件事单独固化成文档，而不是只靠聊天记忆：

- 新增新电脑续学指南
- 明确新电脑需要读哪些文件
- 明确 GitHub 连接方式继续沿用 SSH over 443
- 给出统一的恢复提示词

### 这一步学到的东西

- 真正能保证跨电脑续接的，不是聊天记录本身，而是仓库里的上下文文档
- 只要核心文档和 Git 历史都在，新电脑就能把状态重新恢复回来
- 把“如何恢复上下文”也写成项目文档，本身就是长期学习项目的一部分

### 这一步做出的固化

- 新增 `NEW_MACHINE_SETUP.md`
- 在 `README.md` 和 `PROJECT_CONTEXT.md` 里挂上这份说明
- 以后换电脑时，默认先读：
  - `README.md`
  - `PROJECT_CONTEXT.md`
  - `LEARNING_JOURNAL.md`
  - `TEACHING_FLOW.md`

### 下一步

- 后续如果切换新机器，就直接按这份说明执行
- 文档读完后继续从搜索阶段往下学

## Session 012 - 搜索阶段第一小节

日期：2026-03-14

### 我们这一步在做什么

开始从“建索引”往“怎么把结果搜出来”过渡，先把内容搜索最基础的一小段讲清楚：

- `search.py` 在搜索流程里扮演什么角色
- 为什么内容搜索要从 `document_chunks_fts` 开始
- `snippet(...)` 和 `bm25(...)` 分别负责什么
- 为什么结果里既有片段，也有块编号和行号

### 这一步学到的东西

- `search.py` 是很薄的一层，主要负责清理查询词和转交存储层
- 真正的内容搜索发生在 `storage.py` 的 SQL 里
- 内容搜索不能从 `files` 表开始，因为 `files` 表不保存正文全文索引
- 内容搜索真正搜的是“命中的文本块”，再通过文本块回查所属文件

### 关于结果字段的理解

- `snippet(...)` 的作用是从命中的文本块里截一段更适合展示的片段
- `bm25(...)` 的作用是给每条结果计算相关度分数，用来排序
- `snippet` 偏向展示给人看
- `chunk_index`、`start_line`、`end_line` 偏向给系统和定位使用

### 这一步形成的关键结论

- 当前内容搜索可以概括成一句话：
  - 先找命中的块，再回头找到这个块属于哪个文件
- 当前结果返回可以概括成一句话：
  - 既要让人看懂命中了什么，也要让系统知道命中在哪里

### 下一步

- 继续往搜索阶段推进
- 可以接着看 API 层是怎么把这些搜索结果返回给前端的

## Session 013 - 回到整体方案与学习路线

日期：2026-03-17

### 我们这一步在做什么

这一步没有继续深挖单个函数，而是回到最初的软件方案上，重新判断我们现在到底推进到哪一层了，以及后面最该补什么知识：

- 重新对照最初的 `v1` 软件目标
- 判断已经做成了什么、还没做成什么
- 区分“继续逐行读代码”和“开始推进软件闭环”哪条更值当

### 这一步学到的东西

- 当前后端检索底座已经从 0 到 1 了，不再是空骨架
- 但完整的软件最小闭环还没形成，因为 Python 服务虽然写了，还没有正式跑通对外接口
- 目前最缺的关键知识点不是单个函数细节，而是：
  - `v1` 的整机图
  - Electron 和 Python 服务的衔接方式
  - 从“代码存在”到“软件可用”之间还差什么

### 当前阶段判断

- 已完成：
  - Python 索引服务骨架
  - 内容搜索、文件名搜索、文件级增量索引、根目录查询
  - 这些关键点配套的理解和笔记
- 未完成：
  - FastAPI 依赖安装与服务实跑
  - 接口实测
  - Electron 侧联动
  - 可操作的桌面产品最小闭环

### 这一步形成的路线结论

- 后续不应再长期停留在逐行细讲
- 更合理的路线是：
  - 先讲清 `v1` 整机闭环
  - 再讲清 Electron 和 Python 服务的分层关系
  - 然后把 Python 服务实际跑起来并验证接口

### 下一步

- 下次继续时，优先从整体闭环讲起
- 然后转到实操，把 Python 服务启动并打通

## Session 014 - 先补整机闭环认知

日期：2026-03-20

### 我们这一步在做什么

今天没有继续细抠代码，而是先把最初的软件方案重新压成一个完整闭环，避免只看零件却看不见整机：

- 用户授权目录
- Python 服务建索引
- 用户发起搜索
- Python 返回结构化命中结果
- Electron 展示结果
- 用户执行打开文件或目录

### 这一步学到的东西

- 当前项目主线不是桌面宠物外观，而是“本地文件检索发动机”
- Python + SQLite + FTS5 现在承担的是核心检索底座
- Electron 后续主要承担交互外壳、展示和本地动作执行
- 目前已经做成的是后端检索链路，不是完整可操作桌面产品

### 当前阶段重新判断

- 已基本成形：
  - 索引
  - 内容搜索
  - 文件名搜索
  - 结构化结果返回
- 还没真正完成：
  - 用户授权目录的产品化入口
  - Electron 展示层
  - 点击结果打开文件/目录
  - Python 服务的正式实跑验证

### 这一步形成的结论

- 后续再学，不应继续长期停留在逐行解释
- 更合理的顺序是：
  - 先讲清 Electron 和 Python 服务为什么分层
  - 再把 Python 服务启动起来
  - 然后开始真正把最小软件闭环跑通

### 下一步

- 下次继续时，先讲前后端分层理由
- 然后直接转到服务实跑和接口验证

## Session 015 - 固定服务启动与冒烟测试

日期：2026-03-21

### 我们这一步在做什么

今天没有直接跳去写 Electron，而是先把 Python 服务从“手动跑通过一次”推进到“以后能稳定重复跑”：

- 安装后端依赖
- 真实启动 FastAPI 服务
- 实测最小接口闭环
- 把启动与验收流程固化成脚本

### 这一步做过的验证

- 安装依赖：
  - `fastapi`
  - `uvicorn`
  - `pydantic`
- 成功启动服务：
  - `python -m uvicorn index_service.main:app --app-dir src --host 127.0.0.1 --port 8000`
- 成功验证接口：
  - `GET /healthz`
  - `POST /api/v1/index`
  - `POST /api/v1/search`
  - `POST /api/v1/search/files`
  - `GET /api/v1/roots`

### 这一步学到的东西

- “代码已经写出来”不等于“软件已经可用”
- 真正跨过这条线的标志是：服务能启动、接口能响应、最小链路能跑通
- 启动流程如果只停留在聊天记录里，过几天就要重新回忆；把它固化成脚本，才算真正沉淀
- 现阶段最值得先固定的，不是更多功能，而是稳定的开发入口和验收入口

### 这一步做出的固化

- 新增 `scripts/start_index_service.ps1`
  - 统一启动本地 FastAPI 服务
- 新增 `scripts/smoke_test_index_service.ps1`
  - 统一验证最小接口闭环

### 当前结论

- Python 检索服务已经不是“只存在于代码里”，而是“本地可启动、可验证、可重复运行”
- 这一步做稳后，后面不管是继续补后端，还是接 Electron，都会轻很多

### 下一步

- 继续按“边做边学”的路线推进
- 优先把服务启动/调试流程讲清楚
- 然后再决定是先补后端体验，还是开始接桌面端

### 本轮新增协作约定

- 以后每完成一步有效推进，默认直接 `commit + push`
- 不再需要额外等“现在提交并推送”的提醒

## Session 016 - 把接口整理得更像产品接口

日期：2026-03-21

### 我们这一步在做什么

今天没有继续扩新功能，而是往“后面前端更容易接”这个方向推进了一小步：

- 给健康检查、索引、搜索响应补 `message`
- 给搜索响应补 `total_results`
- 把无效输入的 `400` 错误统一成结构化格式
- 补 API 级自动化测试

### 这一步学到的东西

- 能查出结果，不等于接口已经适合前端直接用
- 真正给前端接时，除了数据本身，还需要：
  - 更清楚的摘要信息
  - 更稳定的错误结构
  - 更明确的结果总数
- 这类整理不是“表面包装”，而是在提前稳定前后端协议

### 这一步做出的固化

- `create_app()` 现在支持注入测试配置，便于 API 级测试隔离数据库
- 搜索与索引响应补了更适合展示的字段
- `400` 错误体统一成：
  - `code`
  - `message`
- 补上了 API 级测试，避免后面改接口时无意破坏对外协议

### 本次验证

- `python -m unittest tests.test_indexing`
- `python -m compileall src tests`
- 启动真实服务后再次跑通：
  - `.\scripts\smoke_test_index_service.ps1`

### 当前结论

- Python 服务现在不只是“能跑”，而是接口也开始更像真正给桌面端消费的产品接口
- 这一步做完后，继续补后端体验或开始接 Electron 都更顺

### 下一步

- 继续补后端体验层，或者
- 开始做一个非常薄的前端调用壳，验证前后端衔接

## Session 017 - 先接一个最小前端壳

日期：2026-03-21

### 我们这一步在做什么

今天没有直接开做正式 Electron，而是先接一个极薄的前端调用壳，把“前端到底怎么调后端”这层先跑通：

- 后端补本地开发用 CORS
- 新增静态网页壳
- 新增前端壳启动脚本
- 验证前后端联通

### 这一步学到的东西

- 从后端到桌面产品，中间还隔着一层“调用壳”
- 这层的核心不是视觉，而是：
  - 前端怎么发请求
  - 后端怎么放行跨域
  - 返回结果怎么被前端消费
- 先做一个薄壳，比一上来直接做完整 Electron 更容易看清联动链路

### 这一步做出的固化

- 新增 `playground/index.html`
  - 用最小网页直接调后端接口
- 新增 `scripts/start_frontend_shell.ps1`
  - 本地启动静态前端壳
- 后端配置新增本地开发 CORS 白名单

### 本次验证

- `python -m unittest tests.test_indexing`
- `python -m compileall src tests`
- 真实后端继续能跑通：
  - `.\scripts\smoke_test_index_service.ps1`
- CORS 预检通过：
  - 允许 `http://127.0.0.1:4173` 调用 `/api/v1/search`
- 静态前端页可访问：
  - `http://127.0.0.1:4173`

### 当前结论

- 现在已经不是只有“后端能跑”，而是“有一个最小前端壳能把后端调用起来”
- 这一步做完后，后面正式接 Electron 时，重点会从“怎么发请求”转到“怎么做桌面交互”

### 下一步

- 继续把这个薄壳往 Electron 迁移，或者
- 先补一个最小的“点击结果后打开文件/目录”动作链

## Session 018 - 把结果点击动作链接上

日期：2026-03-21

### 我们这一步在做什么

今天继续沿着最小前端壳往前推，没有直接开做完整 Electron，而是先把“查到结果之后能干什么”接起来：

- 新增本地动作服务
- 后端新增打开动作接口
- 前端壳结果卡片新增“打开文件 / 打开目录”按钮

### 这一步学到的东西

- 搜索只是找到证据，产品闭环还差“结果后的动作”
- 浏览器本身不能直接替我们打开本地文件，所以要通过后端动作接口中转
- 这层动作链后面迁到 Electron 时，思想不会变，只是执行方会从网页壳逐步转到桌面端

### 这一步做出的固化

- 新增 `src/index_service/actions.py`
  - 统一处理本地打开文件或目录
- 新增 `POST /api/v1/open`
  - 前端通过它触发本地动作
- `playground/index.html`
  - 结果卡片补上动作按钮

### 本次验证

- `python -m unittest tests.test_indexing`
- `python -m compileall src tests`
- 打开动作接口已通过 API 级测试

### 当前结论

- 当前最小前端壳已经具备：
  - 搜索
  - 展示
  - 点击结果触发本地动作
- 这意味着我们离正式桌面产品又近了一步

### 下一步

- 开始把这个薄壳迁到 Electron，或者
- 先继续补前端壳的使用体验

## Session 019 - 把薄壳迁进最小 Electron

日期：2026-03-21

### 我们这一步在做什么

今天没有继续只停留在网页壳，而是正式补了一个最小 Electron 工程，把现有调用链迁进桌面窗口：

- 新增 Electron 入口
- 新增 Electron 启动脚本
- 补本地依赖安装
- 保持后端继续独立运行

### 这一步学到的东西

- Electron 这一层当前最重要的不是框架，而是“桌面宿主”这个角色
- 只要最小调用链已经在网页壳里跑通，迁到 Electron 的第一步就不需要再重做一套前端
- Electron 用 `file://` 加载页面时，要额外考虑后端 CORS 对 `Origin: null` 的放行

### 这一步做出的固化

- 新增 `package.json`
- 新增 `electron/main.js`
- 新增 `scripts/start_electron_shell.ps1`
- `.gitignore` 补了 `node_modules/`
- 后端 CORS 白名单补了 `null`

### 本次验证

- `npm install`
- `npx electron --version`
- `python -m unittest tests.test_indexing`
- `python -m compileall src tests`
- 真实起过后端和 Electron 进程，确认桌面壳可启动

### 当前结论

- 现在已经不只是“有后端 + 网页壳”，而是“有最小桌面宿主”
- 这意味着后面再往前推，就可以开始讨论真正的 Electron 应用结构，而不是先纠结桌面入口怎么落

### 下一步

- 继续把当前薄壳整理成更像 Electron 应用的结构，或者
- 开始接 Electron 侧更正式的前后端启动编排

## Session 020 - 固定桌面栈统一启动编排

日期：2026-03-21

### 我们这一步在做什么

今天继续往“桌面应用开发入口”推进，没有碰桌宠形态，而是先把最小桌面栈的启动方式固定下来：

- 新增统一启动脚本
- 新增统一停止脚本
- 让脚本负责拉起后端、等待健康检查、再拉起 Electron
- 让脚本负责统一关停整套栈

### 这一步学到的东西

- 当后端和 Electron 分开后，真正开始日常开发时，最容易反复浪费时间的就是启动编排
- “能分别启动”不等于“开发入口已经顺手”
- 真正稳的做法是把：
  - 启动顺序
  - 健康检查
  - PID 记录
  - 停止逻辑
 统一收进脚本

### 这一步做出的固化

- 新增 `scripts/start_desktop_stack.ps1`
- 新增 `scripts/stop_desktop_stack.ps1`
- `data/runtime/` 改成运行时状态目录，并加入 `.gitignore`

### 本次验证

- 统一启动脚本可拉起后端和 Electron
- 后端健康检查通过
- 运行状态文件能写入
- 统一停止脚本可关闭整套栈
- 停止后：
  - 状态文件消失
  - 8000 端口不再监听

### 当前结论

- 现在最小桌面栈已经有了正式开发入口
- 后面继续做桌面端时，不需要再回到“今天该先开哪个进程”的问题上

### 下一步

- 继续把 Electron 壳从“承载网页”推进到“更像正式应用结构”，或者
- 开始考虑桌宠形态到底要不要继续共用这套窗口壳

## Session 021 - 先把桌宠入口和搜索窗口拆开

日期：2026-03-23

### 我们这一步在做什么

今天没有继续把 Electron 往传统单窗口应用上堆，而是先把窗口角色拆开：

- 新增桌宠入口壳
- 搜索窗口继续沿用现有前端壳
- 宠物壳负责唤起或隐藏搜索窗口
- 保持统一启动编排可用

### 这一步学到的东西

- “桌宠应用”不等于“只有一个很大的主窗口”
- 更合理的产品结构是：
  - 桌宠壳负责常驻入口
  - 搜索窗口负责完整功能
- 先把角色拆开，比先打磨视觉更重要，因为这会决定后面 Electron 结构怎么长

### 这一步做出的固化

- 新增 `electron/pet.html`
- 新增 `electron/preload.js`
- `electron/main.js` 改成双窗口角色：
  - 宠物壳窗口
  - 搜索窗口
- 宠物壳可直接：
  - 打开搜索窗口
  - 隐藏搜索窗口
  - 退出应用

### 本次验证

- `python -m unittest tests.test_indexing`
- `python -m compileall src tests`
- 统一启动脚本可拉起：
  - Python 后端
  - Electron 宠物壳
  - 搜索窗口宿主
- 统一停止脚本可关闭整套栈

### 当前结论

- 现在 Electron 结构已经不再是“一个普通大窗口”
- 产品方向开始真正往“桌宠入口 + 功能窗口”靠拢

### 下一步

- 继续补桌宠壳和搜索窗口之间更自然的切换方式，或者
- 开始考虑桌宠壳最小交互外观

## Session 022 - 桌宠壳先成形并把搜索改成懒加载

日期：2026-03-23

### 我们这一轮在做什么

这一步不再继续把重点放在搜索窗口上，而是把产品主次真正落到桌宠壳上：

- 先把桌宠壳做成一个可以常驻桌面的最小形态
- 让搜索窗口从“默认跟着启动的主角”降成“被桌宠唤起的能力面板”
- 顺手把统一停机脚本补稳，保证开发时能干净启停整套桌面栈

### 这一轮学到的东西

- “桌宠主、搜索辅”不能只停在口头上，必须反映到窗口生命周期上
- 如果搜索窗口一启动就被创建，它在产品权重上就仍然像主窗口
- 真正更贴近桌宠产品的做法是：
  - 桌宠窗口默认存在
  - 搜索窗口按需创建
  - 桌宠负责表达当前状态和唤起能力
- 开发脚本是否能把进程真正停干净，会直接影响后面桌面端迭代效率

### 这一轮做出的实现调整

- `electron/main.js`
  - 桌宠窗口尺寸和桌面定位进一步固定
  - 搜索窗口改成懒加载
  - 新增桌宠对搜索窗口可见状态的同步
- `electron/preload.js`
  - 暴露桌宠读取当前壳状态的最小桥接能力
- `electron/pet.html`
  - 重做成更像桌面宠物而不是控制面板的小壳
  - 保留打开搜索、隐藏搜索、退出桌宠三项动作
  - 会根据搜索窗口状态更新文案和按钮可用性
- `scripts/stop_desktop_stack.ps1`
  - 改成先停进程、等待退出、再校验端口释放

### 本轮验证

- `powershell -ExecutionPolicy Bypass -File .\scripts\start_desktop_stack.ps1`
- `GET http://127.0.0.1:8000/healthz`
- 校验 Electron 与 Python 进程均被拉起
- `powershell -ExecutionPolicy Bypass -File .\scripts\stop_desktop_stack.ps1`
- 校验状态文件已删除、8000 不再监听

### 当前结论

- 现在项目里已经不是“有个搜索窗口，再挂一个桌宠入口”
- 更接近“先有桌宠，搜索是她暂时已接上的第一项能力”
- 这一步做完后，后续继续赋予桌宠其他能力时，结构上不会再反着长

### 下一步

- 继续补桌宠的存在感和轻交互，而不是优先堆搜索面板
- 再决定下一项要赋予桌宠的能力是什么
